{
  "canonicalRequest" : "POST\nhttps:\/\/api.anthropic.com\/v1\/messages\nanthropic-version: 2023-06-01\ncontent-type: application\/json\nx-api-key: <SECRET>\n\n{\"max_tokens\":64000,\"messages\":[{\"content\":\"Reply with the single word OK.\",\"role\":\"user\"}],\"model\":\"claude-haiku-4-5\",\"system\":[{\"cache_control\":{\"type\":\"ephemeral\"},\"text\":\"Test fixture <UUID-0>. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side.  End fixture.\",\"type\":\"text\"}],\"temperature\":0}",
  "key" : "1bdfbf291094b54abbc3699c85f63417eeb14dc74ba67f22b7a620436914bc30",
  "response" : {
    "data" : "{\"model\":\"claude-haiku-4-5-20251001\",\"id\":\"msg_016YaTNu235TmAouxdaK6twN\",\"type\":\"message\",\"role\":\"assistant\",\"content\":[{\"type\":\"text\",\"text\":\"OK\"}],\"stop_reason\":\"end_turn\",\"stop_sequence\":null,\"stop_details\":null,\"usage\":{\"input_tokens\":13,\"cache_creation_input_tokens\":4640,\"cache_read_input_tokens\":0,\"cache_creation\":{\"ephemeral_5m_input_tokens\":4640,\"ephemeral_1h_input_tokens\":0},\"output_tokens\":4,\"service_tier\":\"standard\",\"inference_geo\":\"not_available\"}}",
    "error" : ""
  },
  "streamChunks" : [

  ],
  "streaming" : false
}