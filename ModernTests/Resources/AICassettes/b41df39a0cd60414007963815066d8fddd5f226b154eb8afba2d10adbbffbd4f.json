{
  "canonicalRequest" : "POST\nhttps:\/\/api.anthropic.com\/v1\/messages\nanthropic-version: 2023-06-01\ncontent-type: application\/json\nx-api-key: <SECRET>\n\n{\"max_tokens\":64000,\"messages\":[{\"content\":[{\"cache_control\":{\"type\":\"ephemeral\"},\"text\":\"Reply with the single word HELLO.\",\"type\":\"text\"}],\"role\":\"user\"}],\"model\":\"claude-haiku-4-5\",\"system\":[{\"cache_control\":{\"type\":\"ephemeral\"},\"text\":\"Test fixture <UUID-0>. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side. This is throwaway filler text only here to push the system prompt over Anthropic's minimum cacheable size for Claude Haiku (~2048 tokens), so the cache_control marker actually engages on the server side.  End fixture.\",\"type\":\"text\"}],\"temperature\":0}",
  "key" : "b41df39a0cd60414007963815066d8fddd5f226b154eb8afba2d10adbbffbd4f",
  "response" : {
    "data" : "{\"model\":\"claude-haiku-4-5-20251001\",\"id\":\"msg_01FjCAX29vT55ewWwbUq2oaM\",\"type\":\"message\",\"role\":\"assistant\",\"content\":[{\"type\":\"text\",\"text\":\"HELLO\"}],\"stop_reason\":\"end_turn\",\"stop_sequence\":null,\"stop_details\":null,\"usage\":{\"input_tokens\":3,\"cache_creation_input_tokens\":11,\"cache_read_input_tokens\":4643,\"cache_creation\":{\"ephemeral_5m_input_tokens\":11,\"ephemeral_1h_input_tokens\":0},\"output_tokens\":5,\"service_tier\":\"standard\",\"inference_geo\":\"not_available\"}}",
    "error" : ""
  },
  "streamChunks" : [

  ],
  "streaming" : false
}